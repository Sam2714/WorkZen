{
  "name": "workzen",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "run-workzen-dev.bat",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix backend run start",
    "preview": "npm run build && npm run start",
    "health": "node -e \"fetch('http://127.0.0.1:4000/health').then(async (response) => { const body = await response.text(); if (!response.ok) { throw new Error(body || ('HTTP ' + response.status)); } console.log(body); }).catch((error) => { console.error('Health check failed. Start the backend with `npm start`, `npm run dev:backend`, or `run-workzen-prod.bat`.'); console.error(error.message); process.exit(1); })\""
  }
}
