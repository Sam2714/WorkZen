export default function App() {
  return (
    <main className="min-h-screen bg-[var(--color-bg)] text-[var(--color-text)]">
      <section className="mx-auto flex min-h-screen max-w-4xl flex-col justify-center px-6 py-16">
        <p className="text-sm uppercase tracking-[0.3em] text-[var(--color-muted)]">
          WorkZen
        </p>
        <h1 className="mt-4 text-4xl font-semibold">Focus scaffold ready.</h1>
        <p className="mt-4 max-w-2xl text-base text-[var(--color-muted)]">
          The repository structure is in place for tasks, focus sessions, insights,
          and the backend API layer.
        </p>
      </section>
    </main>
  );
}
